#use for aliases
echo "running z aliases"

alias home='cd "$(git rev-parse --show-toplevel 2>/dev/null || echo "$HOME")"'
alias sl='ls'
alias ll='ls -l -a'
alias chat='kilo'
alias vi='nvim'
alias gr='grep -r'
alias dotfiles='/usr/bin/git --git-dir=$HOME/documents/dotfiles/ --work-tree=$HOME'
